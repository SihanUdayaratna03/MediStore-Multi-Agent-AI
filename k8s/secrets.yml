# ==============================================================================
# MediStore AI — Kubernetes Secrets (template)
# File: k8s/secrets.yml
#
# ⚠️  WARNING: DO NOT COMMIT REAL VALUES TO GIT ⚠️
# Real secrets are injected by the CI/CD pipeline using:
#   kubectl create secret generic medistore-secrets \
#     --from-literal=GEMINI_API_KEY=<real-value> \
#     --from-literal=GOOGLE_MAPS_API_KEY=<real-value> \
#     --dry-run=client -o yaml | kubectl apply -f -
#
# The values below are base64-encoded PLACEHOLDERS only.
# Generate real base64: echo -n "your-key-here" | base64
# ==============================================================================
apiVersion: v1
kind: Secret
metadata:
  name: medistore-secrets
  namespace: medistore-prod
  labels:
    app: medistore-ai
type: Opaque
data:
  # PLACEHOLDER — replaced by CI/CD pipeline at deploy time
  GEMINI_API_KEY: UEFDS0VHLU5PVC1SRUFMLVZBTFVFCg==
  GOOGLE_MAPS_API_KEY: UEFDS0VHLU5PVC1SRUFMLVZBTFVFCg==
